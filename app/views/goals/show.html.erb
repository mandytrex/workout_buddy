<h1>Goals#show</h1>
<p>Find me in app/views/goals/show.html.erb</p>

<h3> My Current Fitness Goals: </h3>
<% @goals.each do |goal| %>

<% if current_user == @user %>
<p><%= goal.goal_name %></p>
<%= link_to "Edit Goal", edit_goal_path(goal), :class => 'btn btn-default' %>

<% end %>
<% end %>
<%= link_to "Add a New Goal", new_goal_path, :class => 'btn btn-default' %>


